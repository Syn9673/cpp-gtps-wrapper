const fs = require('fs');let i = fs.readdirSync(`${__dirname}/lib/Functions`).filter(e => fs.statSync(`${__dirname}/lib/Functions/${e}`).isDirectory());let res = [];for (let x = 0; x < i .length; i++){let f = fs.readdirSync(`${__dirname}/lib/Functions/${i[x]}`).filter(file => (file.endsWith('.cpp') || file.endsWith('.cc') && fs.statSync(`${__dirname}/lib/Functions/${i[x]}/${file}`).isFile())).forEach(file => res.push(`${__dirname}/lib/Functions/${i[x]}/${file}`));}return res;